import { EventEmitter } from 'events';
export default class WebSocket extends EventEmitter {
    constructor(_url, _stream, _params, _accessToken, _userAgent) {
        super();
    }
    start() { }
    stop() { }
}
